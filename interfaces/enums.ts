export enum UserRoleEnum {
    USER = "USER",
    SHOPPER = "SHOPPER",
    AUTHOR = "AUTHOR",
    ADMIN = "ADMIN",
}

export enum PaymentMethodEnum {
    cash = "cash",
    online = "online",
    credit = "credit",
}

export enum OrderStatusEnum {
    DONE,
    PENDING,
    PROCESS,
    REJECT,
}
